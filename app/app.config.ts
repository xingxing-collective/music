export default defineAppConfig({
  ui: {
    variables: {
      dark: {
        background: 'var(--bg-color)',
        foreground: 'var(--text-color)',
      },
      header: {
        height: '4rem',
      },
      light: {
        background: 'var(--bg-color)',
        foreground: 'var(--text-color)',
      },
      player: {
        height: '4rem',
      },
    },
  },
});
