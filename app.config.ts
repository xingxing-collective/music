export default defineAppConfig({
  ui: {
    variables: {
      light: {
        background: 'var(--bg-color)',
        foreground: 'var(--text-color)',
      },
      dark: {
        background: 'var(--bg-color)',
        foreground: 'var(--text-color)',
      },
      header: {
        height: '4rem',
      },
      player: {
        height: '4rem',
      },
    },
  },
});
